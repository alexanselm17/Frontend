import 'package:flutter/material.dart';

class PayLaterOders extends StatelessWidget {
  const PayLaterOders({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
